# BASIC# Build Handshake v0.1.20

## Project and version

- **Project:** BASIC# Ruby Bootstrap Compiler
- **Build:** v0.1.20 BSharp IR Identity Migration
- **Required base:** accepted v0.1.19 Whole-Number Values and Damage Amounts
- **Required commit:** `a479702`
- **Required tag:** `v0.1.19`
- **Target version:** `v0.1.20`
- **Project path:** `/home/dereksparks1982/DKLab/Projects/BASIC#`

## Exact package

`BASIC_Sharp_Ruby_Bootstrap_Compiler_v0_1_20_BSHARP_IR_IDENTITY_MIGRATION_CHANGED_FILES_ONLY.zip`

## Completed changes

- Renamed the intermediate representation to BSharp Intermediate Representation.
- Established BSharp IR / BSIR names.
- Changed the format marker to `bsir.debug.json`.
- Renamed active `.ir.json` samples and fixtures to `.bsir.json`.
- Renamed active IR contract files to `BSIR_MEANING_CONTRACT_*`.
- Renamed the active patch manifest to `BASIC_SHARP_PATCH_MANIFEST.json`.
- Renamed its format to `BASIC_SHARP_CHANGED_FILES_PATCH`.
- Updated current compiler, runtime, tests, tools, samples, contracts, build records, and documentation.
- Preserved imported Company Bible history.
- Removed retired DKIR execution and added the exact approved recovery message.
- Added converted BSIR fixtures and focused identity tests.
- Preserved all accepted runtime meaning.

## Excluded work

- No DKIR compatibility layer or silent conversion.
- No grammar, Head, official-word, reference, Kind, selection, IF, value, or amount changes.
- No event queue, time, save/load, ASK, arithmetic, bytecode, VM, GUI, editor, engine bridge, or self-hosting work.

## Validation

```text
139 runs
4,739 assertions
0 failures
0 errors
0 skips
```

All five established stress lanes pass.

## Known risks

- Retired DKIR files no longer run. Their original `.bsharp` source must be recompiled.
- Renaming touches many references and paths, so the installer validates payload hashes, base hashes, deletions, current filenames, format markers, tests, stress lanes, and rollback.
- Imported Company Bible filenames deliberately retain historical DK names.

## Rollback point

```text
commit a479702
tag v0.1.19
```

The installer must restore that accepted base if installation or validation fails.

## Continuation

Candidate built and internally validated. Derek must install, inspect, and accept or reject it before commit and tag. After acceptance, the next proposal returns to Event Ordering and Queue Design unless Derek changes direction.
