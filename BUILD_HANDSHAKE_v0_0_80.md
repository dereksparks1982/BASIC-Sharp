# BASIC# Build Handshake v0.0.80

- Project: BASIC# Ruby Bootstrap Compiler
- Version: v0.0.80
- Release: Self-Hosting Milestone 2 Slice 7 - First BASIC#-Authored Compiler Component
- Required base: v0.0.79
- Required base commit: `fdb8d17e6ea48e524cbe8713026a58902bd5da20`
- Required base tag: `v0.0.79`
- Package: `BASIC_Sharp_Ruby_Bootstrap_Compiler_v0_0_80_SELF_HOSTING_MILESTONE_2_FIRST_BASIC_SHARP_AUTHORED_COMPILER_COMPONENT_CHANGED_FILES_ONLY.zip`
- Rollback point: v0.0.79 at `fdb8d17e6ea48e524cbe8713026a58902bd5da20`

## Completed scope

- Add the first bounded compiler-domain decision component authored in BASIC# at `compiler/native/first_bsharp_compiler_component.bsharp`.
- Compile that BASIC# source through the accepted independent compiler driver and pipeline into a real persisted `first_bsharp_compiler_component.bsbc` artifact and readable disassembly.
- Execute the persisted artifact through the independent BSBC loader and independent BSharp VM after the compilation source copy is removed.
- Classify all nine accepted compiler block heads: `KINDS`, `DEFINE`, `START`, `WHEN`, `IF`, `OTHERWISE`, `CONTROLS`, `HOVER`, and `CONTEXT`.
- Prove byte-identical repeated compilation, deterministic compiler decisions, disabled production compiler/runtime constructors on the primary proof path, and separate production/Ruby referee parity.
- Repair the canonical Company Bible header from the stale v0.0.77 value to v0.0.80 and machine-check the header against `BasicSharp::VERSION`.
- Preserve Profiles 1 through 7, the accepted BSBC layout, production routing, creator-facing BASIC# syntax, the opening `(` action-word visual guide, and written action order.

## Changed files

The authoritative changed-path inventory is `docs/changed_files/BASIC_SHARP_CHANGED_FILES_v0_0_80.txt` and `BASIC_SHARP_PATCH_MANIFEST.json`.

## Validation

Build-side complete normal and no-locale suites pass at 660 runs / 9,998 assertions / 0 failures / 0 errors / 0 skips. Acceptance additionally requires every sealed validation tool, the first-native-component gate, deterministic fixture sweep, release package preflight, release forensic overlay, whole-language gauntlet, full-count Trial by Fire, final version check, and unmistakable `FINAL PASS`.

## Known risk and rollback

The critical risk is overstating self-hosting by treating a BASIC# demonstration as compiler ownership. The native component therefore performs a real bounded compiler-domain classification task, is compiled into persisted BSBC, executes source-free, and must match separate production and Ruby referees. Any failed candidate returns to accepted v0.0.79 before a repaired v0.0.80 package is applied.

## Continuation

After installer `FINAL PASS`: accepted snapshot first, then local commit/tag, then GitHub push and peeled-tag verification. Do not begin v0.0.81 before v0.0.80 is fully closed.
