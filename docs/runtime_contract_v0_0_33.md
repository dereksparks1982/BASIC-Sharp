# BASIC# runtime contract v0.0.33

The preferred BSharp VM and Ruby reference runtime must produce identical world state, actions, IF settlement, follow-up events, ASK inspection, hover output, context menus, context execution, and Save documents for Meaning Profile 3. Runtime code remains engine-neutral.
