# BASIC# comment syntax v0.1.33

`//` opens a comment and `/.` closes it. Comments may span lines; periods inside them are punctuation. Markers inside quoted text are text. Comments cannot nest. An unmatched close or missing close is a compile error, and an unclosed comment reports the opening line and column.
