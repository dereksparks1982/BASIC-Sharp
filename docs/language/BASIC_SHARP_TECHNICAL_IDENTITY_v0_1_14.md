# BASIC# Technical Identity v0.1.14

## Official project identity

```text
Displayed language and project name: BASIC#
Pronunciation and written-out name: Basic Sharp
Ruby namespace: BasicSharp
Safe lowercase identifier: basic_sharp
Creator source extension: .bsharp
```

The `#` belongs in the real creator-facing name. `BasicSharp` and `basic_sharp` are technical fallbacks only where a programming language, filename convention, or tool cannot safely use `#`.

## Historical identity

The former working name is retired from active compiler names, project filenames, creator source extensions, banners, and current documentation. It remains inside clearly marked historical records, package names, Git commits, and audit notes where changing it would falsify project history.

## Folder identity

The active local project folder is:

```text
/home/dereksparks1982/DKLab/Projects/BASIC#
```

Shell commands must quote this path because `#` begins a shell comment when unquoted.
