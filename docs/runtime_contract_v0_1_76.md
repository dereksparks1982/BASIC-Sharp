# BASIC# Runtime Contract v0.1.76

v0.1.76 changes the bounded self-hosting compiler lane, not creator-facing runtime meaning. `SmallCompilerSubsetBSBCLoader` must independently decode and validate accepted Profile 1-7 BSBC into a trusted model equivalent to the production Ruby `BytecodeLoader` referee, while rejecting the sealed malformed-artifact campaign with matching diagnostics. A bootstrap-only VM adapter may feed the independently loaded model into the existing BSharp VM execution engine. Save, ASK, input, creator-facing runtime behavior, and the BSBC binary format remain unchanged.
