# BSIR game input and interaction contract v0.1.33

Meaning Profile 3 adds ordered `controls`, `hover_declarations`, and `context_declarations` arrays. References are resolved to built-in PLAYER, named objects, Kinds, or scene-local `it`. Comments and visual markers never enter resolved identities. Line numbers remain diagnostic metadata and are excluded from meaning fingerprints.
