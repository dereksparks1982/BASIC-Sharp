# BASIC# Runtime Contract v0.0.63

v0.0.63 does not change runtime behavior.

The build adds a self-hosting bridge lane for emitting BSBC bytes from approved small compiler subset fixtures under Ruby referee control. Profiles 1 through 7, BSharp bytecode, the bytecode VM, runtime transition, save/restore, ASK, input, movement, and Trial by Fire behavior remain under existing validation gates.
