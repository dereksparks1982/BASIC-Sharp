# BASIC# Runtime Contract v0.1.55

v0.1.55 does not change runtime behavior.

The build adds a small compiler subset symbol table contract and documentation records only. Profiles 1 through 7, BSharp bytecode, the bytecode VM, runtime transition, save/restore, ASK, input, movement, and Trial by Fire behavior remain under existing validation gates.
