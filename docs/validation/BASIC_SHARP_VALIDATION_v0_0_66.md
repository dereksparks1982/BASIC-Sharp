# BASIC# Validation v0.0.66

Candidate validation must include:

```text
Ruby syntax
JSON parsing
sealed validation inventory
complete test suite
all installer-order stress tools
small compiler subset BSBC execution parity
small compiler subset execution corpus
release package preflight
deterministic fixture hash sweep
Trial by Fire inventory
Trial by Fire gauntlet
input device contract
```

Expected installer acceptance line:

```text
BASIC# v0.0.66 installation and full native validation: PASS
```
