# BASIC# Validation v0.1.70

Expected native validation:

- Full Ruby test suite passes with no failures, errors, skips, warnings, or stderr.
- Release forensic overlay passes before project mutation.
- Deterministic fixture hash sweep passes.
- Trial by Fire passes with expanded v0.1.70 default counts.
- Input device, movement intent, and package preflight gates remain active.
