# BASIC# Validation v0.1.34

## Required native acceptance result

```text
minimum runs: 369
minimum assertions: 7,692
failures: 0
errors: 0
skips: 0
Ruby warnings/stderr: 0
```

Every established stress, Meaning Profile, Bytecode Contract, Emitter, Loader, VM, runtime transition, text value, comment, Demon Killer input, and game interaction lane must pass. `tools/bytecode_emitter.rb` must confirm Profile 3 game emission through `BytecodeEmitter#profile`. Profile 1 and Profile 2 committed BSBC and disassembly hashes must remain unchanged. The package must match the exact manifest scope and restore accepted v0.1.32 if any post-install gate fails.

The owner-side native installer result remains the acceptance authority. Do not commit or tag v0.1.34 before that result passes and Derek accepts the build.
