# BASIC# v0.1.84 Validation Record

## Build-side evidence

```text
Normal suite:    692 runs, 10190 assertions, 0 failures, 0 errors, 0 skips
No-locale suite: 692 runs, 10190 assertions, 0 failures, 0 errors, 0 skips
87 test files
75 required tools
```

Native parser dispatch, semantic routing, symbol resolution, and new action routing integration tools passed build-side, including fixed-point and fail-closed proofs. Deterministic fixture hash sweep, README current-release truth, bootstrap boundary audit, validation inventory, and release forensic overlay also passed build-side.

The owner-side installer still must run the complete sealed required-tool inventory, package preflight against the actual Git working tree, and the canonical full-count Trial by Fire before one unmistakable `FINAL PASS`.

## Rejected candidate repair gate

The first v0.1.84 package was rejected after `tools/company_bible_audit.rb` found the roadmap missing the canonical Company Bible path. The repaired v0.1.84 must pass `tools/company_bible_audit.rb` before handoff. This failure does not advance the version and v0.1.83 remains the accepted base until v0.1.84 reaches `FINAL PASS`.

## Redo findings repaired before handoff

During the complete v0.1.84 redo from accepted v0.1.83, additional stale version-bearing evidence was found before delivery and repaired instead of weakening any validator:

- runtime expected fixtures for number changes, compound IF, OTHERWISE, and creator-facing text Save hashes;
- the independent BSBC loader and VM fixture IR/Save digests;
- the canonical self-hosting contract's Slice 11 status, current milestone ledger, and native action-routing document links;
- the Company Bible audit itself, updated to enforce Derek's approved self-contained `.sh` development-build rule rather than the retired routine ZIP wording.

After those repairs the normal suite passed `692 runs, 10190 assertions, 0 failures, 0 errors, 0 skips`; the no-locale suite passed the same counts; the Company Bible audit passed; and all required tools executed build-side except the monolithic full-count Trial by Fire, which exceeded the build host command window during its 128,000-event phase. The self-contained owner-side installer retains the canonical unsharded full-count Trial by Fire as a mandatory gate before `FINAL PASS`.
