# BASIC# Runtime Contract v0.1.75

v0.1.75 changes the bounded self-hosting compiler lane, not creator-facing runtime meaning. `SmallCompilerSubsetBSBCEncoder` must independently emit bytes that are exactly equivalent to the production Ruby `BytecodeEmitter` referee for the accepted subset corpus. The existing BytecodeLoader, BSharp VM, Save, ASK, input, and Profile 1-7 runtime behavior remain governed by their existing contracts and parity gates. The BSBC binary format is unchanged.
