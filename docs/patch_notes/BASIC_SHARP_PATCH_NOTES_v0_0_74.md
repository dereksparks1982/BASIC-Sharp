# BASIC# Patch Notes v0.0.74

Adds an independent semantic resolver to BSharp Compiler Subset 0. The subset compiler now resolves its own bounded semantic document before IR emission, while the production Ruby resolver remains a separate parity referee. The execution corpus grows to 18 fixtures and 56 events, including v0.0.73 object interactions.
