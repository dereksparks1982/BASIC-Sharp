# BASIC# Patch Notes v0.0.75

Adds an independent BSBC byte encoder to BSharp Compiler Subset 0. The bounded subset now generates its own BSBC bytes while the production Ruby `BytecodeEmitter` remains a separate exact-parity referee. The execution corpus grows to 19 fixtures and 57 events, including Profile 7 object interaction plus numeric and IF/OTHERWISE behavior.
