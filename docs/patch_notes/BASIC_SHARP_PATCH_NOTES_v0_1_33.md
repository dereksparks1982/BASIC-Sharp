# BASIC# v0.1.33 patch notes

BASIC# scripts now have visible landmarks: uppercase `PLAYER`, `#` for Kinds, `@` for particular objects, and `|then` for results. Comments can span lines between `//` and `/.`. Demon Killer scripts can declare keyboard/mouse controls, hover fields, and state-aware context actions. Existing program meaning remains stable after marker resolution.
