# BASIC# Patch Notes v0.0.63

v0.0.63 adds the Small Compiler Subset Runtime Smoke lane. The small compiler subset now proves selected fixtures can reach the verifying runtime, run smoke events, produce deterministic snapshots, and produce deterministic save documents under Ruby referee control.
