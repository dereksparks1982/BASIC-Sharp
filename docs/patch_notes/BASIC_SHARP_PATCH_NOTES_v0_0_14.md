# BASIC# Patch Notes v0.0.14

BASIC# now carries its correct name all the way down to the machinery.

Use:

```bash
ruby compiler/basic_sharp.rb samples/first_room.bsharp
```

The compiler and runtime now display BASIC#. The Ruby namespace is `BasicSharp`, creator source files use `.bsharp`, and the project folder becomes `BASIC#`.

The complete Company Bible is now stored inside the project docs. No new language feature was added in this build. Existing v0.0.13 behavior and saved BSharp IR remain compatible.
