# BASIC# Patch Notes v0.1.77

Adds an independent BSharp VM execution engine to the bounded Self-Hosting Milestone 2 lane. `SmallCompilerSubsetBSBCVirtualMachine` executes independently loaded trusted models while the production `BytecodeVirtualMachine` and `BasicSharp::Runtime` remain exact referees. The corpus grows to 21 fixtures and 60 events, with 1,024-event deterministic and follow-up-loop boundary proof.
