# BASIC# Patch Notes v0.0.42

v0.0.42 completely re-carries Trial by Fire after v0.0.40 and v0.0.41 each correctly stopped at stale version-bearing runtime fixtures and rolled back to exact v0.0.39.

All four version-sensitive fixtures are now regenerated and protected together: creator-facing text Save, number changes, compound IF, and OTHERWISE. Their validators remain fully enforced. The canonical Company Bible now requires this complete inventory audit before future numbered packages are sealed.

Trial by Fire adds no new ammunition. It takes every accepted BASIC# Profiles 1–7 system to the range. One large program exercises the whole language against a hand-locked expected history. A recorded seed creates 256 reproducible valid programs. Four hostile mutation boundaries reject 8,192 corrupted artifacts, and the bytecode loader must reject every truncated prefix. Native stress phases hammer events, movement, ASK, Save/restore, isolated worlds, reactive behavior, and the 1,024 follow-up safety boundary.

The language remains exactly Profiles 1–7. The committed bytecode/disassembly artifacts remain byte-identical. No accepted-semantics defect required a runtime or compiler repair.
