# BASIC# Patch Notes v0.0.73

Adds executable plain-English object interaction actions: `(open`, `(close`, `(lock`, and `(take`. They lower onto existing state-change and carry behavior so the creator gets friendlier words without a new bytecode profile.
