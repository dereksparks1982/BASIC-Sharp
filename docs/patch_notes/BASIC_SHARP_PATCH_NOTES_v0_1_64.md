# BASIC# Patch Notes v0.1.64

v0.1.64 is a discipline build with real compiler progress.

It expands BSBC execution parity and adds package preflight armor so stale fixture hashes, sealed inventory byte drift, and manifest mistakes are caught before handoff.

No DKLab bridge removal, BCS implementation, syntax change, Profile 8, or full self-hosting claim is included.
