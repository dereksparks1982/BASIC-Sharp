# BASIC# Patch Notes v0.0.76

Adds an independent BSBC loader to BSharp Compiler Subset 0. The bounded subset now decodes and validates its own BSBC while the production Ruby `BytecodeLoader` remains a separate exact-parity referee. The execution corpus grows to 20 fixtures and 58 events, and a 16-mutation malformed-artifact campaign proves the new loader as a real trust boundary. The repaired candidate also corrects stale Slice 2 canonical README current lines and strengthens the README truth gate to validate those exact lines across the whole document.
