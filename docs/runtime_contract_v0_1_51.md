# BASIC# Runtime Contract v0.1.59

v0.1.59 changes no runtime semantics.

The build adds a non-production small compiler subset IR golden parity harness. Runtime behavior, Save/restore, ASK, BSharp Bytecode, BSharp VM, input devices, movement, comments, profiles, and creator-facing syntax remain unchanged.
