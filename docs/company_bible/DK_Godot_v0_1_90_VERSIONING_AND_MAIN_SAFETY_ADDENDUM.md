# Company Bible Addendum - Versioning and Main Safety

Version: v0.1.90
Date: 2026-07-02

## Versioning Rule

After `v0.1.99`, DK does not continue to `v0.1.100`.

The next version after `v0.1.99` is:

```text
v0.2.01
```

Reason: Derek considers the project progress ready to roll into the 0.2 line after the 0.1.99 milestone.

## Main.tscn Insurance Policy Reminder

For every DK build or patch that touches or includes `scenes/Main.tscn`, make a backup copy of Main before making further changes.

The backup must be clearly labelled with the version/build context.

## Current Build Note

This v0.1.90 package does not include or overwrite `scenes/Main.tscn`.

This was done deliberately to avoid deleting or replacing Derek's current map cells and local scene work.
