# Company Bible Addendum - v0.4.38

## Gaia Streaming Cell vocabulary

Use **Gaia Streaming Cell** for the large parent cell layer above existing seamless child streaming cells.

A Gaia Streaming Cell is a parent planning/runtime unit. It is not the same thing as the current detailed region cell.

Current contract:

```text
1 Gaia Streaming Cell = 5 x 8 child streaming cells = 40 child streaming cells.
```

## Image-generation permission rule

Planning, questions, corrections, confirmations, and visual discussion are not permission to generate or edit images.

Only generate or edit an image when Derek gives a clear direct command to make/create/edit the picture.

## Patch manifest rule reminder

Do not invent the top-level `DK_PATCH_MANIFEST.json` schema. Use the accepted `DK_CHANGED_FILES_PATCH` format version 1 unless the installed Companion/Build Integrator is intentionally changed by Derek.
