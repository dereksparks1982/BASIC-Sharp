# BSharp ASK Number Changes Contract v0.1.37

ASK reads Profile 5 worlds without mutation. Existing questions such as `what values does player have`, `what IF rules are true`, event inspection, world summaries, and save summaries must report current whole-number values and canonical threshold conditions identically in the BSharp VM and reference runtime.
