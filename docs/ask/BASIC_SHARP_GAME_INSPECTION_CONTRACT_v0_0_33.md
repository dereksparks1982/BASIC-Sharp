# BASIC# game inspection contract v0.0.33

`ASK "what game systems are declared"` returns complete, deterministic `CONTROLS`, `HOVER`, and `CONTEXT` declarations without executing actions or changing the world. Source, saved BSIR, and loaded BSBC must agree. Human output reports declaration counts; JSON retains every declaration.
