# BSharp Save contract v0.1.33

Meaning Profile 3 uses Save format version 3 and `sha256-bsir-meaning-v3`. The fingerprint includes resolved controls, hover declarations, and context declarations, but excludes comments, visual markers, raw spelling, and line numbers. Context-caused world changes restore identically in the reference runtime and VM.
