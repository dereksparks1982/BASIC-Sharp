# BASIC# Runtime Contract v0.0.72

v0.0.72 does not change creator-facing runtime behaviour.

The release only repairs test-side CLI capture encoding under no-locale Ruby and refreshes version-bearing validation records. Existing runtime behaviour, BSBC execution parity, input mapping, movement intent, ASK, Save, and BSharp VM behaviour remain protected.
