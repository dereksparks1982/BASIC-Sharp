# BASIC# Build Handshake v0.1.47

## Build identity

- Version: v0.1.47
- Title: Tokenizer/Reader Contract and Universal Standard Doctrine
- Required base version: v0.1.46
- Required base commit: `b4d8ea2c5274e63cab3de6e6fcf003e2aef25a35`
- Required base tag: `v0.1.46`
- Required branch: `main`
- Required tree state: clean
- Package: `BASIC_Sharp_Ruby_Bootstrap_Compiler_v0_1_47_TOKENIZER_READER_CONTRACT_CHANGED_FILES_ONLY.zip`

## Purpose

Define the BASIC# tokenizer/reader contract for the self-hosting runway and document the long-term BASIC# / BSharp universal-standard, AI-tooling, web-compatibility-first, and performance doctrine.

## Scope

This build adds:

- tokenizer/reader JSON contract;
- tokenizer/reader documentation;
- tokenizer/reader validator tool;
- tokenizer/reader test;
- universal-standard and AI-tooling doctrine;
- updated self-hosting foundation links;
- updated Company Bible, roadmap, README, master handoff, runtime contract, validation, changelog, patch notes, session log, and changed-files record;
- live version truth advanced to `0.1.47`.

## Explicit exclusions

No Profile 8, new creator syntax, runtime behavior change, BSharp IR change, BSharp Bytecode change, Save format change, ASK behavior change, input-device behavior change, engine bridge, web export, browser work, licensing, monetization, funding claim, OpenAI outreach, Project Oracle work, BASIC# Semantic Oracle implementation, or Ruby replacement.

## Rollback

The installer verifies the exact v0.1.46 base before mutation. If validation fails after mutation, it restores every modified v0.1.46 file and removes every added v0.1.47 path.

## Validation plan

- Ruby syntax check.
- JSON parse check.
- Sealed Trial-by-Fire validation inventory check.
- Complete test suite.
- Every required tool in the Trial-by-Fire validation inventory.
- Full native Trial-by-Fire gauntlet on Derek's machine during installation.
- Manifest scope check against Git status.

## Acceptance

v0.1.47 is accepted only after Derek's machine reports installer PASS, then Derek commits the tree and tags `v0.1.47`.
