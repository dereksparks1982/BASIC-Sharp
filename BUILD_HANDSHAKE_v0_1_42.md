# BASIC# Build Handshake v0.1.42

## Identity

- Target: v0.1.42 Trial by Fire Complete Re-Carry, Complete Versioned Runtime Fixture Repair, and BSharp VM Hardening.
- Required base: commit `066e715`, tag `v0.1.39`, clean branch `main`.
- Package: `BASIC_Sharp_Ruby_Bootstrap_Compiler_v0_1_42_TRIAL_BY_FIRE_COMPLETE_RE_CARRY_COMPLETE_VERSIONED_RUNTIME_FIXTURE_REPAIR_AND_BSHARP_VM_HARDENING_CHANGED_FILES_ONLY.zip`.
- Scope: exactly 24 modified and 28 added project paths; zero deletions.
- Meaning: Profiles 1–7 unchanged; no Profile 8.
- Repair: re-carry the complete failed Trial-by-Fire work, regenerate all four version-sensitive runtime fixtures together, and add the canonical Company Bible rule requiring complete version-bearing fixture audits before packaging.

## Installer promises

1. Verify exact Git base, tag, branch, clean tree, manifest identity, path counts, base hashes, payload hashes, and protected artifact hashes before mutation.
2. Install only manifest-listed files.
3. Print every validation phase and reject any warning, stderr, failure, error, or skip.
4. Verify all JSON, Ruby syntax, exact test inventory, at least 448 runs and 8,012 assertions, every established tool, all four repaired runtime-fixture gates, and the full-count Trial-by-Fire gauntlet.
5. Restore every v0.1.39 byte and remove every new v0.1.42 path after any post-mutation failure.
6. On success, leave only the declared v0.1.42 candidate and instruct Derek to commit and tag it.

Native owner validation is the acceptance gate. Ruby-WASM preflight does not replace it.
