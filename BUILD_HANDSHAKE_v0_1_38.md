# BASIC# Build Handshake v0.1.38

## Identity

- Target: v0.1.38 Plain-English Compound IF Conditions and BSharp Profile 6.
- Required base: commit `8eb1fbe`, tag `v0.1.37`, clean branch `main`.
- Package: `BASIC_Sharp_Ruby_Bootstrap_Compiler_v0_1_38_PLAIN_ENGLISH_COMPOUND_IF_CONDITIONS_AND_BSHARP_PROFILE_6_CHANGED_FILES_ONLY.zip`.
- Scope: 48 modified, 53 added, 0 deleted; 101 project paths.

## Installer promises

1. Verify package identity, manifest counts, direct-root payload hashes, exact base hashes, Git commit/tag/branch, and clean tree before mutation.
2. Save replaced base files outside the project.
3. Install only the manifest's modified and added paths; no deletions are permitted.
4. Run Ruby syntax, JSON parsing, the complete test suite with no warnings/stderr, every established validation tool, Meaning/Bytecode Profile 6 lanes, compound IF stress, Company Bible audit, and Profile 1–5 compatibility hashes.
5. On any post-mutation failure, restore every replaced v0.1.37 byte and remove every newly added path and empty directory.
6. On success, leave only the exact manifest scope in the Git worktree and instruct Derek to commit and tag v0.1.38.

No engine bridge, rendering, editor, IDE, or excluded language feature is included.
