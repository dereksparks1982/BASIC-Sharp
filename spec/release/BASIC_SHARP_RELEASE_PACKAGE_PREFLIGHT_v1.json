{
  "format": "bsharp.release_package_preflight.json",
  "format_version": 1,
  "target_version": "0.1.65",
  "status": "active_release_gate",
  "title": "BASIC# Release Package Preflight",
  "purpose": "Verify the active changed-files manifest, installed tree bytes, release identity, validation inventory inclusion, and package handoff records before a candidate ZIP is entered into acceptance validation.",
  "required_manifest": "BASIC_SHARP_PATCH_MANIFEST.json",
  "required_installer": "APPLY_BASIC_SHARP_v0_1_65.sh",
  "required_base_version": "v0.1.64",
  "required_target_version": "v0.1.65",
  "required_package_suffix": "_CHANGED_FILES_ONLY.zip",
  "required_tools": [
    "tools/deterministic_fixture_hash_sweep.rb",
    "tools/release_package_preflight.rb",
    "tools/small_compiler_subset_execution_corpus.rb"
  ],
  "forbidden": [
    "handing Derek a ZIP whose final extracted payload has not been audited",
    "changing sealed validation inventory after package records are generated",
    "repairing only the first failed deterministic fixture hash",
    "removing the DKLab compatibility bridge in v0.1.65",
    "claiming BASIC# is fully self-hosted"
  ]
}
