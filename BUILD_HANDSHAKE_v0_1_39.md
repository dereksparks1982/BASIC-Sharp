# BASIC# Build Handshake v0.1.39

## Identity

- Target: v0.1.39 Plain-English OTHERWISE Branches and BSharp Profile 7.
- Required base: commit `3a92d4e`, tag `v0.1.38`, clean branch `main`.
- Package: `BASIC_Sharp_Ruby_Bootstrap_Compiler_v0_1_39_PLAIN_ENGLISH_OTHERWISE_BRANCHES_AND_BSHARP_PROFILE_7_CHANGED_FILES_ONLY.zip`.
- Scope: 58 modified, 53 added, 0 deleted; 111 project paths.

## Installer promises

1. Verify the exact Git base, package identity, counts, base hashes, and payload hashes before mutation.
2. Install only manifest-listed modified and added paths; no deletions are permitted.
3. Run Ruby syntax, JSON parsing, the complete suite, all established tools, Profile 7 conformance, bytecode compatibility, and alternating-branch stress.
4. Restore every v0.1.38 byte and remove every new v0.1.39 path after any post-mutation failure.
5. On success, leave only the declared candidate changes and instruct Derek to commit and tag v0.1.39.

The language pair is IF / OTHERWISE. `ELSE` is not an alias. No engine, rendering, editor, IDE, or excluded language feature is included.
